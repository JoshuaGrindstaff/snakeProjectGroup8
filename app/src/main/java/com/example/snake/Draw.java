/*

package com.example.snake;

import android.content.Context;
import android.graphics.Canvas;
import android.graphics.Paint;
import android.view.SurfaceHolder;

public class Draw
{

    private Paint mPaint;

    private HUD hud;
    private GameObjects gO;
    private volatile boolean mPaused = true;
    Draw(Context context)
    {

    mPaint = new Paint();

}
    public void update(boolean mPaused, int mScore,SurfaceHolder mSurfaceHolder,Snake mSnake,Apple mApple,Canvas mCanvas)
    {

        hud.update(mPaused, mScore, mSurfaceHolder,mPaint,mCanvas);

        gO.update(mSurfaceHolder,mPaint,mSnake,mApple,mCanvas);

    }


}

*/
